# Binaries
bin/
*.exe
*.dll
*.so
*.dylib

# Test binary
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work.sum

# Dependency directories
vendor/
node_modules/

# Local-only reference material (not part of distribution).
# The upstream source is available via `git fetch upstream` since we're a fork.
/refs/